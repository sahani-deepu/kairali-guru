# Kairali Guru — Delta pack (additions & corrections)
### Apply these on top of the base build spec you already have · v1 → v2

*Self-contained. Share just this file. It captures everything added/changed after the packaged build spec: the two-location correction, the chatbot, the conversion + ad-MCP layer, the contact/how-to-reach/enquiry pages, the image guide, and the build-hygiene/integrity/deploy notes. Section refs (§) point to the full spec.*

---

## 0. Delta execution prompt (paste into Antigravity)

```
Apply the following deltas to the kairali.guru build (they update the base spec in instructions/kairali_guru_build_spec.md). The base spec still governs; these override or add to it. Read each delta, then adjust or add the affected pages/config. Do not machine-translate; never fabricate data — stub and ask where a value is missing.

Δ1  Two locations only — Kerala + Delhi. Remove Gurgaon and Mumbai everywhere (location pages, Google Business Profiles, prose). Location pages = /locations/kerala and /locations/delhi. Add a "Preferred location: Kerala / Delhi / Either" field to the enquiry form. How-to-reach content is per location.
Δ2  Add the Chatbase AI advisor (§24) — consent-gated, AI-disclosed, WhatsApp/CRM handoff, multilingual + RTL, lead → conversion event, credit-usage alerts.
Δ3  Add the conversion layer (§25.1): server-side GTM + Meta Pixel/CAPI + Google Enhanced Conversions, all consent-gated. (The ad-management MCPs in §25.2 are an ops layer, not a website task.)
Δ4  Build the Contact / How-to-Reach / Enquiry pages (§26) reusing the proven KTAHV patterns: minimal-friction form (Name + one of phone/email + consent + honeypot), real WhatsApp links, NO exit/timed popups, noindex + no-dead-end thank-you. Use the verified Kerala data below; Delhi details are TODO.
Δ5  Follow the image guide (§28): never AI-generate or hotlink images; use supplied first-party assets or approved royalty-free stock; where missing, insert a labelled placeholder with the brief (subject + filename + alt); follow the naming + alt-text conventions; localize alt per locale.
Δ6  Install the taste-skill with: npx skills add Leonxlnx/taste-skill
Δ7  Self-host — do NOT deploy to Vercel (§29): Next.js standalone/Docker or static export, behind Cloudflare, on Kairali infra. GitHub for source + CI only. Approve no paid hosting/domain step.
Δ8  Repo hygiene (§29): private repo; .gitignore all secrets/.env/keys; never commit uploaded brand docs or image originals; scan before every push; account IDs via env vars.
Δ9  Content integrity (§29): never invent faculty, credentials, accreditations, testimonials, curricula, dates, fees, coordinates, or distances — stub and ask.

Work phase by phase; validate each phase (schema, Lighthouse ≥95, a11y/WCAG 2.2 AA, hreflang, mobile + RTL) before advancing.
```

---

## 1. The deltas in detail

### Δ1 — Two locations only: Kerala + Delhi *(correction)*
The programme runs in **exactly two locations — Kerala (the healing village) and Delhi.** Gurgaon and Mumbai were sales offices, not training venues → removed. Effects: location pages = `/locations/kerala` + `/locations/delhi`; **Google Business Profiles = Kerala + Delhi**; the enquiry form gains a **"Preferred location: Kerala / Delhi / Either"** field; how-to-reach is per location; "From Delhi to Kerala" is now literally accurate.

### Δ2 — Chatbase AI advisor *(new · §24)*
Add a Chatbase agent (RAG on your site content, 24h auto-retrain; SOC 2 + GDPR + CCPA + DPA). Persona = "Kairali Ayurveda course advisor," clinical-accurate, **gives no medical advice** (school, not clinic). **Handoff = WhatsApp** (`wa.me/919289838797`) + CRM (no native live-chat inbox). **Lead capture → CRM + GA4 `chatbot_lead` conversion.** Multilingual EN/DE/FR/AR/RU + **RTL**. **AI-disclosure** (EU AI Act). **Consent-gated** load + domain allowlist. Style to brand tokens; never auto-open aggressively. **Monitor credit usage** (silent-failure mode).

### Δ3 — Conversion + ad-MCP layer *(new · §25)*
- **Website (build it):** server-side GTM backbone + **Meta Pixel + Conversions API** (server-side) + **Google Ads Enhanced Conversions**, all **consent-gated** (Consent Mode v2). Send enquiry_submit / prospectus_download / chatbot_lead server-side.
- **Ops layer (not a website task):** official **Meta Ads MCP** (read+write, OAuth) + official **Google Ads MCP** (read-only, needs developer token + OAuth2 + Google Cloud project) — wire into your n8n/agent stack; writes gated to approval.

### Δ4 — Contact / How-to-Reach / Enquiry pages *(new · §26 — reuse KTAHV patterns)*
- **How-to-Reach (per location, indexable):** hero + arrival cards (air/rail/road) + pickup block + **lazy-loaded Google map** (real coords, Get-directions + View-larger buttons) + distances table + address + directional FAQ + mobile WhatsApp/Call bar. Schema: `EducationalOrganization` + `GeoCoordinates` + `hasMap` + `openingHours` + `BreadcrumbList` + `FAQPage`.
  - **Kerala verified data (reuse directly):** coords **10.727275, 76.710507**; Coimbatore (CJB) **≈62 km/~1 hr**; Kochi (COK) **≈115 km**; Palakkad Junction (PGT) **≈18 km**; NH544; venue = the **NABH teaching hospital/village**. **Delhi:** address + phone + coords + hours = **TODO (send)**.
- **Enquiry + thank-you:** required = Name + one of phone/email + **Preferred location** + Programme of interest + **required consent** + honeypot; **"what happens next" 3-step**; WhatsApp = **real link** recording consent; **click-triggered callback only — NO exit/timed popup**; events `enquiry_start`/`enquiry_submit`(conversion). **Thank-you: `noindex,follow`, no dead-end** (WhatsApp/Call + "while you wait" links), hide Enquire button, privacy line.
- **Hardening (carry over every KTAHV fix):** prefix all IDs/classes + null-safe JS; FAQ JSON-LD generated from visible FAQ verbatim; WhatsApp green `#128350` (4.79:1) hover `#0F7A47`; self-host fonts; reciprocal hreflang (declare `og:locale:alternate` only where the page exists); one canonical `/contact`; modal a11y (focus trap/return, aria-modal, Escape). **Constants:** WhatsApp **+91 92898 38797** · IVR **+91 9555156156** · **info@kairali.com**.

### Δ5 — Image & media guide *(new · §28)*
**AI never picks/generates images.** Priority: **first-party photography > licensed stock > botanical line-art.** Provides: the "Kairali look" art direction, a **banned-clichés** list (generic spa/lotus/AI-fake), **file-naming convention** (`kairali-{topic}-{subject}-{location}.webp`, keyword-first, WebP/AVIF + responsive), **alt-text formula** (`{who} {doing what}, at Kairali's {location}`, localized per locale), technical specs (≥2400px heroes, LCP priority, ImageObject schema), **per-slot manifest by archetype**, **free-license sources** (Unsplash/Pexels/Pixabay/StockCake + paid for released people), and a **first-party shot list** for Kerala + Delhi (with model-release/consent = DPDP requirement). Missing image → **labelled placeholder + brief.** Curated **free-license candidate links** (mapped to slots) are in **`IMAGE_LINKS_2026-07-09.md`** — interim only; verify license + download from source; first-party preferred.

### Δ6 — taste-skill install *(new · §21/§29)*
`npx skills add Leonxlnx/taste-skill` (this is the "taste-skill"; run once, use for design).

### Δ7 — Self-host, not Vercel *(new · §29)*
Antigravity defaults to Vercel — **override.** Self-host: Next.js **standalone (Node/Docker)** or **static export** behind **Cloudflare** on Kairali infra. GitHub = source + CI only; deploy target = Kairali's server. Approve **no** paid hosting/domain step.

### Δ8 — Repo hygiene *(new · §29)*
**Private repo.** `.gitignore` all secrets (`.env*`, keys, tokens, service-account JSON, CMP/analytics/ad credentials) + uploaded brand docs + image originals. **Scan before every push.** Account IDs via **environment variables**, never plaintext.

### Δ9 — Content integrity *(new · §29)*
**Never fabricate** faculty, credentials, accreditations, testimonials, curricula, hours, dates, fees, coordinates, or distances → labelled placeholder + ask. (Fake credentials/reviews = legal + E-E-A-T risk; fake Review schema can trigger a Google manual action.)

---

## 2. Still needed from you (unchanged)
**Delhi training-centre details** (address, phone, coordinates, hours — Kerala already verified) · **account IDs** (GA4/GTM/Clarity/GSC/Bing/Yandex+Metrica/CMP/Cloudflare) · **CRM/form endpoint + WhatsApp + ESP** · **Chatbase workspace** (or approval to create) · confirm **Meta CAPI + server-side GTM** · **faculty roster + accreditation wording + course dates/fees** · point me to your **image library**. Everything else stubs and continues.
